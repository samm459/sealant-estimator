import React from "react"
import { render } from "@testing-library/react"
import { Estimator } from "./estimator"

describe("Estimator", () => {
    it("works", () => {
        render(<Estimator />)
    })
})
